以下の問い合わせ内容を分析し、タグスキーマをJSON形式で出力してください。

# 問い合わせ内容
{{#start.inquiry_text#}}

# 制約(厳守)
- core軸はちょうど1個。
- detail軸は最大 {{#start.max_detail_axes#}} 個まで。これを超えてはならない。
- 各軸の candidates には必ず「不明」または「該当なし」を含める。
- candidates は問い合わせの製品カテゴリに即した語彙にする(見本のコピー禁止)。
- JSONのみを出力(説明文やコードブロック記法は不要)。
